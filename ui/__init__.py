"""Native macOS (AppKit / PyObjC) user interface for Serato Migrator.

The business logic lives entirely in the toolkit-free modules at the repo root
(`serato_db`, `scanner`, `copier`, `metadata_editor`). This package only builds
the interface and calls into them.
"""
